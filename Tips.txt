git add (filename) = for specific
git add .  = add all files 
git status = git information
git commit -m "(message)" = for commit

Need to commit first and then Add.

Branches 
git checkout -b (branch name) = make new name branch and  switch branch from master branch. 
git checkout (branch name) = switch branch
git merge (branch name) = updates data from one to another branch